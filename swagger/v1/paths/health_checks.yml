get:
  summary: Health check
  tags: [Health Check]
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              health_check:
                type: string
                example: API is running
